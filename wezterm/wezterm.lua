local wezterm = require 'wezterm';

return {
  color_scheme = "ayu",
  enable_tab_bar = false,
  font = wezterm.font("Fira Code"),
  font_size = 10.0,
  window_padding = {
    left = 8,
    top = 8,
    right = 8,
    bottom = 8
  }
}
